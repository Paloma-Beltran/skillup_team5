Subir foto con firebase storage
Al registrar el usuario, poner foto: "https://ui-avatars.com/api/?name=${usuario.nombre}&background=555&color=fff&uppercase=true"
Al modificar el perfil:
    - Si existe una foto en storage, no se actualiza ese link con el avatar por defecto
    - Si no, cambiar el link con su nuevo nombre

Poner input file para las fotos del storage
Al guardar perfil, si no se puso ninguna foto nueva, se ignora y no hace nada
Si subió foto, hasta que se le de a guardar la sube a storage
Poner boton para quitar la foto (vuelve a poner el link con su nombre)




Poner el rol admin directo desde firebase
Hacer que pueda hacer de todo



Agregar validación de correo
// Info email template https://support.google.com/firebase/answer/7000714
sendEmailVerification(auth.currentUser)
.then(() => {
    // Email verification sent!
    // ...
});


NO ESTOY SEGURO
Por ahora solo se puede editar el estado de la publicación y no la publicación completa

Para editar una publicación:
En el perfil de las empresas, mostrar todas las ofertas o cursos que ofrece
Solo esa misma empresa puede ver botones para editar cada publicación, el boton debe tener el id del documento
Crear otro formulario para editar la publicación y al guardar se actualiza en la base de datos